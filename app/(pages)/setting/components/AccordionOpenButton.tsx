type AccordionOpenButtonProps = {
  button: JSX.Element;
};

const AccordionOpenButton: React.FC<AccordionOpenButtonProps> = ({ button }) => {
  return <>{button}</>;
};

export default AccordionOpenButton;
